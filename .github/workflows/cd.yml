name: CD — Build & Deploy

on:
  push:
    branches: [main]

concurrency:
  group: cd-${{ github.ref }}
  cancel-in-progress: false

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    name: Build & Push Multi-arch Image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract metadata (tags, labels)
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push multi-arch image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: build-and-push
    if: github.ref == 'refs/heads/main'
    environment: production

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          port: 10202
          script: |
            set -e
            cd ${{ secrets.DEPLOY_PATH || '~/reviewmind' }}

            # Pull latest code
            git pull --ff-only

            # Build images on the server (no-cache ensures fresh src/ layers)
            docker compose -f docker-compose.yml -f docker-compose.prod.yml build --no-cache

            # Stop current app containers before migration
            docker compose -f docker-compose.yml -f docker-compose.prod.yml stop api bot worker beat flower

            # Run DB migrations (one-off container, uses the freshly built image)
            docker compose -f docker-compose.yml -f docker-compose.prod.yml run --rm api alembic upgrade head

            # Recreate and restart ALL services with the new images
            docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --force-recreate --remove-orphans

            # Wait for API health check
            echo "Waiting for API health..."
            for i in $(seq 1 30); do
              if curl -sf http://localhost:8000/health > /dev/null 2>&1; then
                echo "API healthy after ${i}s"
                break
              fi
              sleep 1
            done
            curl -sf http://localhost:8000/health || echo "WARNING: API health check failed"

            # Cleanup dangling images
            docker image prune -f
